
```bash
git config --global --unset user.name
```

```bash
git config --global --unset user.email
```

Depois, vá no "Gerenciador de Credenciais" do Windows e remova qualquer entrada relacionada ao git.

```bash
git config user.name "vorbitter"
```

```bash
git config user.email "vorbitter@proton.me"
```